'use client'

// This component is now redundant since TranslationContext reads from useParams directly
// Keeping it for backwards compatibility but it's a no-op
export default function LocaleSetter() {
  return null
}

